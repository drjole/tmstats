<h1>All games</h1>

<% if @games.none? %>
  There are not games yet.
  <% if policy(Game).create? %>
    <%= link_to "Create a new one.", new_game_path %>
  <% end %>
<% end %>

<%= render partial: "games/games", locals: { games: @games, user: nil } %>
