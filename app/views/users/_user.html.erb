<%= image_tag user.profile_picture.variant(:thumb) if user.profile_picture.attached? %>
<address>
  <%= mail_to user.email_address %>
  <% if policy(user).update? %>
    |
    <%= link_to "Edit profile", user == Current.user ? profile_edit_path : edit_user_path(user) %>
  <% end %>
</address>
